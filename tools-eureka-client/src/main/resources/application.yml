eureka:
  client:
    service-url:
      defaultZone: http://springcloud-tools:8761/eureka/
server:
  port: 8900
spring:
  application:
    name: eureka-client
  main:
    allow-bean-definition-overriding: true