#服务名
spring:
  application:
    name: tools-uaa
  # 配置中心url
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:9999
        group: DEFAULT_GROUP
        prefix: tools-uaa
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:9999
        namespace: public
  profiles:
    active: dev